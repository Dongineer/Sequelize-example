const Insert = require('./create')
const Delete = require('./destroy')
const Select = require('./find')
const Update = require('./update')

module.exports = {
  Insert,
  Delete,
  Select,
  Update,
}
